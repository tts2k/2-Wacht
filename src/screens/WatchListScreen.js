import React, { useState } from "react";
import { View, Text, StyleSheet } from "react-native";
import { colors } from '../styles';

export const WatchListScreen = () => {
    return (
        <View>
            <Text style={{ fontSize: 30, color: colors.foreground }}>Watch List Screen</Text>
        </View>
    )
}
