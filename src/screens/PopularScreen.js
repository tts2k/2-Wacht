import React from "react";
import {View, Text, StyleSheet} from "react-native";
import { colors } from '../styles';

export const PopularScreen = () => {
    return (
        <View>
            <Text style={{ fontSize: 30, color: colors.foreground }}>Popular Screen</Text>
        </View>
    )
}
