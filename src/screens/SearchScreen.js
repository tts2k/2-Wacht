import React from "react";
import {View, Text, StyleSheet} from "react-native";
import { colors } from '../styles';

export const SearchScreen = () => {
    return (
        <View>
            <Text style={{ fontSize: 30, color: colors.foreground }}>Search Screen</Text>
        </View>
    )
}
