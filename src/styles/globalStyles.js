import { colors }from "./colors"

const popupStyles = {
    optionsContainer: {
        backgroundColor: colors.background,
    },
    optionText: {
        paddingVertical: 10,
        color: colors.foreground,
    },
}

export { popupStyles }
